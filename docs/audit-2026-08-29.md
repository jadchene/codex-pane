# Codex Pane comprehensive audit

Date: 2026-08-29  
Branch: `codex/comprehensive-audit-improvements`

## Scope and method

The review covered the Electron trust boundary, IPC and URL handling, local storage, diagnostic output, dependency exposure, rendering cost, desktop usability, keyboard and screen-reader access, visual hierarchy, failure recovery, build configuration, tests, and release validation. Three review passes were used: baseline inspection, implementation and regression, then a second threat/performance/UX review of the changed product.

## Findings and resolutions

| Area | Finding | Severity | Resolution |
| --- | --- | --- | --- |
| External navigation | Renderer code could request a direct open and bypass native confirmation | High | Removed the bypass; only credential-free HTTP(S) URLs are accepted and every external open requires native confirmation |
| Electron surface | Webviews, navigation by drag/drop, and several runtime-capability fuses were not explicitly disabled | High | Denied webview attachment, disabled drag/drop navigation, and enabled ASAR integrity/only-ASAR loading while disabling RunAsNode, NODE_OPTIONS, and CLI inspector arguments |
| Sensitive data | Diagnostic and tool-output redaction rules were inconsistent | High | Centralized main-process redaction and covered API keys, Bearer values, JWTs, refresh tokens, secret assignments, URL query secrets, and Windows user-profile paths |
| Renderer resilience | Arbitrarily deep or large tool payloads could cause expensive recursive rendering | Medium | Added depth, node-count, and array-entry budgets with a visible truncation marker |
| Disk usage | Managed non-image attachments had no retention or total-size policy | Medium | Added a 30-day retention policy and 1 GiB oldest-first quota; reused files refresh their access timestamp |
| Path validation | Several stores relied on platform-specific prefix checks | Medium | Replaced them with resolved parent-directory containment checks |
| Conversation navigation | Scrolling away from the tail had no discoverable way to return | Medium | Added a persistent “回到最新” action and clearer context-use copy |
| Session accessibility | Session rows were pointer-oriented and not native keyboard controls | Medium | Converted the primary row action to a native button with focus styling and current-item semantics |
| Status/recovery | Connection state was easy to miss and recovery depended on finding a title-bar control | Medium | Added a title-bar status indicator and an inline reconnect action in the error alert |
| Visual hierarchy | Absolute black/white palettes flattened surfaces and increased visual harshness | Low | Introduced layered neutral surfaces, improved conversation spacing, focus treatment, and floating-action contrast |
| Form accessibility | Key controls lacked explicit accessible names and the live transcript lacked log semantics | Low | Added accessible names, live-log semantics, and a bounded message length |

## Existing strengths retained

- Context isolation, sandboxing, disabled Node integration, web security, restrictive content security policy, navigation interception, IPC sender checks, protocol validation, atomic persistence, virtualized conversations, and batched deltas were already present.
- Media storage already enforced age and capacity limits; the review aligned ordinary attachment behavior with that model.
- Markdown content continues to pass through DOMPurify and external URLs are handled outside the renderer.

## User-flow follow-up review

A further three-pass review followed the real task sequence in both workspace modes: enter the app, locate or create a conversation, compose with `/`, `@`, and `$`, attach context, send or steer a running task, read a long response, switch sessions, collapse navigation, and return to the multi-pane layout.

| User-flow problem | Improvement |
| --- | --- |
| Workspace mode switching was buried in Settings and entering the sidebar released idle panes | Added an always-visible title-bar mode switch plus `Ctrl+Shift+M`; mode changes preserve every pane and the selected layout |
| A sidebar-only active session could disappear when returning to panes | The active session is moved into the visible pane range and keeps focus |
| Sidebar navigation reduced reading width | Added a collapsible 42 px rail so the current conversation can temporarily use almost the full window |
| `$` file references were discoverable only from placeholder text | Added a dedicated `$` action beside `/`, `@`, and attachments |
| Empty/loading/error suggestion states looked like a broken menu | Added explicit file-search progress, no-match, failure, and no-Skill rows |
| Skill and file results were difficult to distinguish | Skill descriptions and file name/path pairs are shown in every result |
| Escape deleted the active composer token | Escape now closes suggestions without modifying the draft |
| Clicking `/` with an existing draft could risk destructive replacement | The draft is preserved and a concise explanation is shown |
| A text-only model silently disabled sending with an image attached | The composer explains the incompatibility and how to recover |
| The working directory looked informative but not actionable | It is now a clearly focusable, clickable directory-switch action |
| Empty panes offered no guidance | Empty states now explain the three input accelerators without dominating narrow panes |
| Assistant and user messages had nearly identical card treatment | Assistant responses use an open reading surface; user messages retain a compact accent-tinted bubble |
| Raw reasoning levels and an English working state leaked through | User-facing effort levels and activity status are localized consistently |
| Unknown context usage incorrectly appeared as 0% | Unknown usage is omitted; 80% and 95% thresholds receive warning and critical emphasis |
| Session titles alone were insufficient to distinguish similar work | Renamed sessions also show a one-line content preview, with clearer scope and empty-state copy |
| Narrow panes accumulated oversized controls | Container-based layout rules compact spacing and allow model/effort controls to use a dedicated row |

## Verification

| Check | Result |
| --- | --- |
| Unit/component/security tests | 29 files, 166 tests passed |
| TypeScript and Vue type checking | Passed |
| Electron preload/main build | Passed |
| Renderer production build | Passed |
| electron-builder configuration schema | Passed |
| Dependency audit | 0 known vulnerabilities, including production-only audit |
| Diff whitespace validation | Passed |
| Playwright desktop E2E | Blocked by the audit host: no X server or `xvfb-run` |
| Packaged Windows smoke test | Not executable on the Linux audit host |

## Protocol and keyboard follow-up review

A further protocol-level review mapped every app-server server request to an explicit interactive, automatic, or safe-reject presentation and documented how the supported client requests and notification classes appear in the conversation. The complete matrix is in [protocol-ui-coverage.md](protocol-ui-coverage.md).

This pass replaced raw approval JSON with readable command actions, file targets, network destinations, permission scopes, and persistence warnings; fallback details now redact credential-like fields. It also added terminal-style prompt history, `Ctrl+P`/`Ctrl+N`, reverse menu navigation, Skill-description search, actionable status counters, numbered panes with `Alt+1` through `Alt+6`, and session-sidebar keyboard controls. Unknown future item types now identify their concrete protocol type instead of appearing only as “运行详情”.

## Three-round operational and UI re-review

The final three passes followed keyboard-only and pointer workflows again in both workspace modes, then reviewed every changed feedback path for security, noise, and persistence risk.

1. **Action correctness and recovery:** keyboard submission now honors the same disabled state as the Send button; inactive panes no longer leave suggestion menus or file searches active; warnings, model reroutes, verification, safety buffering, and environment disconnects are shown as readable conversation notices.
2. **Keyboard and visual continuity:** approval headings and decision actions remain visible while long request details scroll; session lists support Arrow, Home, End, and Escape; Escape returns from the sidebar to the conversation; status counters and selected composer options have clear hover/focus/active treatment.
3. **Noise, privacy, and mode switching:** repeated protocol notices coalesce, ordinary initial environment connections stay silent, reconnects remain visible, legacy command arrays render as shell-like commands, and credential-like command values are redacted. Prompt history survives pane/sidebar component remounts in a bounded in-memory cache and is deliberately not added to persisted workspace data.

## Residual risks and release gates

1. Run `npm run verify:release` on Windows before publishing. This validates the real Codex app-server probe, packaged executable, and Windows desktop smoke path.
2. Perform one manual visual pass at 100%, 125%, and 150% display scaling, including keyboard-only navigation and a screen reader smoke test. Automated component coverage cannot fully judge native window chrome or typography at OS scaling.
3. The main UI chunk is about 643 kB uncompressed (about 181 kB gzip), close to the configured warning threshold. Startup remains bounded, but future feature growth should use route/component-level lazy loading rather than increasing the limit.
4. Code signing and an authenticated update channel remain distribution responsibilities; neither is configured in this repository.

## Recommended release command

```powershell
npm ci
npm run verify:release
```

## 2026-08-30 continuation: 25 review-and-improvement rounds

The branch received a further 25 independent review rounds from the perspective of a person installing the app, starting work, running and supervising tasks, responding to privileged requests, recovering from failures, and leaving the app. Each round ended in a code change plus an automated check, or in a recorded release gate where the host could not exercise the Windows-native path.

| Round | User flow or boundary | Finding and completed improvement |
| ---: | --- | --- |
| 1 | Clean install and baseline | Reinstalled from the lockfile, ran the complete unit/security/component suite, production build, whitespace check, and dependency audit before changing behavior. |
| 2 | Entering and leaving full screen | Added `F11`, `Esc`, and a discoverable floating exit action so hiding the custom title bar can no longer strand the user in full screen. |
| 3 | Closing with a fresh draft | Added an immediate, serialized workspace flush before the custom close action; a failed save keeps the window open with a recoverable pane error. |
| 4 | Reconnecting after a Codex failure | Coalesced concurrent reconnect attempts, converted rejection into persistent connection state and a notice, and disabled the title-bar action during start/restart. |
| 5 | Understanding connection compatibility | The title-bar status now distinguishes an unverified Codex version and exposes the full connection message and version in a tooltip. |
| 6 | Opening and searching session history | Added drawer-local loading and error presentation with stale-request suppression instead of placing errors behind the drawer. |
| 7 | Obtaining support information | Added a Settings diagnostics section that copies the last 200 already-redacted records and shows the exact connection/version context. |
| 8 | Answering Codex questions | Removed click-to-submit behavior from choice questions; selection and submission are now separate, preventing accidental answers. |
| 9 | Responding to MCP elicitation | Removed click-to-submit behavior from single-option forms and retained explicit Submit, Decline, and Cancel actions. |
| 10 | Validating MCP form content | Added required/empty-array, string length, regex, integer, numeric range, and array count checks before emitting a response; integer controls now enforce integer precision. |
| 11 | Reviewing command approvals | Replaced ambiguous backslash-escaped argv display with PowerShell-safe single-quoted rendering and expanded JWT, Bearer, URL-query, and secret redaction. |
| 12 | Adding several attachments | A bad file no longer discards successful imports: the UI keeps valid files and reports a bounded per-file failure summary. |
| 13 | Attachment import responsiveness | Limited imports to three concurrent operations, preserving selection order without launching twenty large reads at once. |
| 14 | Attachment disk limits | Media and ordinary-file retention/quota enforcement now runs after imports, not only on the next launch; concurrent maintenance requests coalesce. |
| 15 | Missing managed images | The private media protocol now returns a deterministic non-cacheable 404 and records a redacted warning instead of failing the protocol handler. |
| 16 | Corrupt or hostile persisted state | Added an 8 MiB pre-parse limit plus bounded pane/thread/model/path fields and selector-safe pane IDs. |
| 17 | Renderer-to-main request complexity | Added depth, node, and cycle limits while translating managed attachments, preventing pathological structured-clone payloads from exhausting the main process. |
| 18 | Diagnostic payload complexity | Added depth and node budgets before diagnostic serialization so an unusual protocol object cannot amplify into an expensive log write. |
| 19 | Credential and profile-path privacy | Broadened Bearer token syntax coverage and made Windows profile redaction case- and separator-insensitive. |
| 20 | MCP Gateway presentation | Gateway target extraction now accepts `gateway_call_tool` and `call_tool`, camelCase and snake_case argument names, and non-hard-coded gateway server names; Settings explains that this is display-only. |
| 21 | Inspecting commands before a first prompt | `/mcp`, `/skills`, and `/permissions` no longer create an empty persisted conversation merely to show information. |
| 22 | Sending the first prompt | Restored the visible running state immediately after thread creation so the composer does not appear idle while `turn/start` is in flight. |
| 23 | Session timestamps | Normalized seconds/milliseconds and show “时间未知” instead of a misleading 1970 relative date when app-server omits the timestamp. |
| 24 | Pointer and splitter affordances | Restored pointer/not-allowed cursors for actions and directional resize cursors for pane splitters, which had been overridden by the desktop-wide default cursor rule. |
| 25 | Failure containment and test signal | Added a redacted native fatal-start dialog, caught external-link launch failures in the conversation UI, derived protocol version text from the manifest baseline, and enabled automatic Vue wrapper cleanup to eliminate leaked observers from test teardown. |

### Continuation verification

| Check | Result |
| --- | --- |
| Unit/component/protocol/security suite | 29 files, 171 tests passed after the continuation |
| TypeScript, Vue, Electron and renderer production build | Passed |
| Dependency audit | 0 known vulnerabilities |
| Diff whitespace validation | Passed |
| Renderer chunks | Main application about 236 kB / 73 kB gzip; shared UI about 643 kB / 181 kB gzip |
| Playwright Electron E2E | Test launch attempted; blocked by the Linux audit host having no X server or `$DISPLAY` |
| Windows packaged and live Codex checks | Remain release gates; they require a Windows host with the real Codex CLI |

## 2026-08-30 second continuation: 22 user-journey review rounds

This continuation repeated the audit from distribution through shutdown. It followed what a user sees and can recover from at each step rather than treating individual components as isolated code units.

| Round | User step | Finding and completed improvement |
| ---: | --- | --- |
| 1 | Downloading the app | Replaced the unpacked-only release target with an assisted NSIS installer while retaining the unpacked diagnostic build. |
| 2 | Choosing whether to install | Added a non-elevated portable executable target and independent installer, portable, and unpacked scripts. |
| 3 | Understanding release artifacts | Updated both READMEs with the intended audience and command for each Windows artifact. |
| 4 | Checking the installed version | Exposed the trusted Electron application version through bootstrap and displayed it beside the Codex version in diagnostics. |
| 5 | First visual launch | Added a system theme that follows Windows color-scheme changes live without discarding explicit light/dark choices. |
| 6 | Recovering from appearance experiments | Added a one-click reset for theme, typography, accent, command display, and Gateway presentation settings. |
| 7 | Selecting the default workspace | Added loading exclusion and an inline recoverable error when choosing or clearing the default directory fails. |
| 8 | Restoring moved projects | Invalid persisted global and pane directories are checked concurrently with a bounded wait, then cleared with a precise warning instead of blocking startup or passing stale paths to Codex. |
| 9 | Diagnosing startup failure | Expanded the connection failure surface with a concrete CLI check, diagnostics entry point, and reconnect action. |
| 10 | Reading accumulated warnings | Shows the pending notification count and provides an explicit clear-all action instead of making earlier warnings reappear without context. |
| 11 | Loading session history | Keeps sidebar load failures in the sidebar and provides an in-place retry rather than placing the error behind the current conversation. |
| 12 | Scanning many sessions | Uses sets for working/unread membership, labels running sessions, and explains the 100-result boundary so search does not appear incomplete. |
| 13 | Typing a `$` file reference | Stops launching a broad empty fuzzy search, explains when a query or root is needed, and preserves debounced stale-result suppression. |
| 14 | Entering a slash command | Unknown slash commands are no longer silently sent as ordinary prompts; the composer preserves the text and explains both recovery options. |
| 15 | Preparing attachments | Shows the current 20-item quota, local image size, and a clear limit state; the attachment action disables at capacity. |
| 16 | Sending while disconnected | The composer now explains the connection dependency and prevents a doomed request instead of optimistically clearing the user's work. |
| 17 | Changing live thread settings | Directory, model, and effort controls lock while a turn or approval is active; the store independently rejects a racing directory change. |
| 18 | Receiving safety and setup events | Added readable Guardian warnings, Windows sandbox completion/failure, and failed Hook presentation with bounded detail. |
| 19 | Following plans and context | Added incremental `turn/plan/updated` rendering and explicit context-compaction feedback with stale token usage cleared. |
| 20 | Handling server-side lifecycle changes | Added close, archive, unarchive, and delete behavior; deletion safely resets the pane and explains why. |
| 21 | Refreshing changed Skills | `skills/changed` refreshes once per distinct working directory and fans results to matching panes, avoiding six duplicate requests. |
| 22 | Closing via Alt+F4 or window chrome | All native close paths now request and await a serialized renderer workspace flush before task interruption, confirmation, or shutdown. |

### Second-continuation verification

| Check | Result |
| --- | --- |
| Unit/component/protocol/security suite | 30 files, 178 tests passed |
| TypeScript, Vue, Electron and renderer production build | Passed |
| Dependency audit | 0 known vulnerabilities |
| Diff whitespace validation | Passed |
| Windows unpacked packaging | Passed on the Linux audit host |
| NSIS configuration and packaging pipeline | Configuration and packaging stages passed; final Windows resource step is blocked because the Linux host has no Wine |
| Live app-server probe | Missing Codex CLI is now reported cleanly without an unhandled process error; live compatibility remains a Windows release gate |
| Playwright Electron E2E | Remains blocked on this host because there is no X server or `$DISPLAY` |
